- name: loop playbook
  hosts: all
  become: yes
  tasks:
    - name: printing variable in loop 
      package:
        name: "{{item}}"
        state: present
      loop:
        - apache2
        - nginx
        - mariadb-server
        - tree